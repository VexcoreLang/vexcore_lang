[package]
name = "repl"
version = "0.1.0"
edition = "2021"

[dependencies]
rustyline.workspace = true
lexer = { path = "../lexer" }
parser = { path = "../parser" }
interpreter = { path = "../interpreter" }
